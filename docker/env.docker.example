# Docker 環境變數配置範例
# 複製此檔案到專案根目錄並重命名為 .env，或直接在 docker-compose.yml 中使用

# 應用程式設定
APP_NAME=SignIn
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://signin.example.com

# 資料庫設定（使用 SQLite）
DB_CONNECTION=sqlite
DB_DATABASE=/var/www/html/database/database.sqlite

# 快取設定
CACHE_DRIVER=file
SESSION_DRIVER=file
QUEUE_CONNECTION=sync

# 郵件設定（根據需求調整）
MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="noreply@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# 日誌設定
LOG_CHANNEL=stack
LOG_LEVEL=error

# 檔案系統
FILESYSTEM_DISK=local

